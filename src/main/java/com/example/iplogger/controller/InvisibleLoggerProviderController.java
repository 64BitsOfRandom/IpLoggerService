package com.example.iplogger.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * Контроллер отвечает за генерацию "невидимого" логера - генерируется ссылка на изображение.
 * При переходе по ссылке данные загружаются в логер
*/
@RestController
public class InvisibleLoggerProviderController {
}
