package com.example.iplogger.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * Контроллер отвечает за генерацию "невидимого" логгера - генерируется ссылка на изображение.
 * При переходе по ссылке данные загружаются в логгер
*/
@RestController
public class InvisibleLoggerProviderController {
}
